# Voltage Labs Environment Variables
# Copy this file to .env.local for local development

# ============================================
# Cloudflare R2 Bucket Configuration
# ============================================

# PUBLIC_R2_BUCKET_URL - URL to your Cloudflare R2 bucket for serving images
#
# Options:
# 1. Custom domain (recommended): https://assets.voltagelabs.com
# 2. R2.dev subdomain: https://pub-xxxxxx.r2.dev
# 3. Workers custom domain: https://your-bucket.your-domain.workers.dev
#
# Leave empty to use local /public/ assets during development
# Set in Cloudflare Pages dashboard for production deployment
#
# Example values:
# PUBLIC_R2_BUCKET_URL=https://assets.voltagelabs.com
# PUBLIC_R2_BUCKET_URL=https://pub-abc123def456.r2.dev
# PUBLIC_R2_BUCKET_URL=
#
PUBLIC_R2_BUCKET_URL=

# ============================================
# Setup Instructions
# ============================================
#
# Local Development:
# 1. Copy this file: cp .env.example .env.local
# 2. Leave PUBLIC_R2_BUCKET_URL empty to use local assets, OR
# 3. Set PUBLIC_R2_BUCKET_URL to your R2 bucket URL to test production setup
#
# Cloudflare Pages Production:
# 1. Go to Cloudflare Pages dashboard
# 2. Select your project > Settings > Environment Variables
# 3. Add: PUBLIC_R2_BUCKET_URL = https://your-bucket-url.com
# 4. Redeploy your site
#
# R2 Bucket Requirements:
# - Public read access enabled OR custom domain configured
# - CORS configuration allowing GET requests from your domain
# - Recommended: Use custom domain for better performance and branding
#
# CORS Example Configuration:
# {
#   "AllowedOrigins": [
#     "https://voltage-labs.pages.dev",
#     "https://yourdomain.com",
#     "http://localhost:5173"
#   ],
#   "AllowedMethods": ["GET"],
#   "AllowedHeaders": ["*"],
#   "MaxAgeSeconds": 3600
# }
